/** The single shared column for every project-article block — header text,
 *  tags, metrics, hero image, story prose, gallery, and CTA all align to the
 *  same left/right margin (one consistent "notebook" edge) and share this width.
 *  Vertical padding stays local to each section. */
export const CASE_CONTAINER = "mx-auto w-full max-w-6xl px-6 md:px-12";
