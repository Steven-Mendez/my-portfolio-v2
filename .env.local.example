# Copy this file to `.env.local` and fill in the value.
#
# Groq API key for the portfolio chatbot (server-side only — never exposed to the
# browser). Get a free key (no credit card) at https://console.groq.com/keys
GROQ_API_KEY=
